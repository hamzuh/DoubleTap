extends Area2D

func _physics_process(delta: float) -> void:
	# Fly forward
	# in an arc? up and over?
	pass
	
func hit(killer, weapon, damage, knockback, instakill):
	# Called when bullet hits this
	# Kertwang into nearest enemy
	pass
